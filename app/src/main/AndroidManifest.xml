<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 无障碍服务权限 -->
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE" />
    <!-- 悬浮窗权限 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- 前台服务权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- 高级悬浮窗权限（系统级别） -->
    <uses-permission android:name="android.permission.INTERNAL_SYSTEM_WINDOW"
        tools:ignore="ProtectedPermissions" />
    <!-- 触摸事件权限 -->
    <uses-permission android:name="android.permission.INJECT_EVENTS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidAutoClick"
        tools:targetApi="31">
        
        <!-- 主Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AndroidAutoClick">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 点击位置配置Activity -->
        <activity
            android:name=".ClickPointConfigActivity"
            android:exported="false"
            android:theme="@style/Theme.Material3.DayNight.Dialog" />

        <!-- 无障碍服务 -->
        <service
            android:name=".service.AutoClickService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- 悬浮窗服务 -->
        <service
            android:name=".service.FloatingWindowService"
            android:exported="false" />

        <!-- 点击位置管理服务 -->
        <service
            android:name=".service.ClickPointService"
            android:exported="false" />

        <!-- 悬浮控制面板服务 -->
        <service
            android:name=".service.FloatingControlPanelService"
            android:exported="false" />

        <!-- 添加功能选择器服务 -->
        <service
            android:name=".service.AddActionSelectorService"
            android:exported="false" />

        <!-- 最小化悬浮控制器服务 -->
        <service
            android:name=".service.FloatingMiniControllerService"
            android:exported="false" />

    </application>

</manifest>
